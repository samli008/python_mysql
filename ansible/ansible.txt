# ansible basic not service only tool
yum -y install ansible

# Inventory
vi /etc/ansible/hosts
[mfs]
c0[3:5]

[mfs01]
192.168.20.14[3:4]
192.168.20.151

ansible -i /etc/ansible/hosts mfs -u root -m command -a 'date' -k

ansible mfs -a 'date'

