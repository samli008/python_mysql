1. docker.yaml
2. etcd.yaml
3. apiserver.yaml
4. controller_manager_scheduler.yaml
5. node.yaml
6. sh label; swapoff -a
7. load.yaml
8. kubectl apply -f nginx.yaml
9. kubectl apply -f nginx-service.yaml
10. kubectl apply -f dashborad.yaml

etcdctl member list
kubectl get cs
kubectl get node

export ETCDCTL_API=3
etcdctl get / --prefix --keys-only
etcdctl get /registry/deployments/default --prefix --keys-only
